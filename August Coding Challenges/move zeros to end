class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int i,j ;
        for(int i=0;i<nums.size();i++){
             if(nums[i]==0){
                int j=i;
                 break;
             }
        }
        
       j=0;
       for(int i=j+1;i<nums.size();i++){
          if(nums[i]!=0){
            swap(nums[j],nums[i]);
             j++;  
           }
           
        }
    
        
    }
};
